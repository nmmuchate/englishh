---
phase: 06-firebase-auth
task: 0
total_tasks: 3
status: planned
last_updated: 2026-02-24T00:00:00.000Z
---

<current_state>
Phase 6 planning is 100% complete. All 3 plan files have been written and verified. No execution has started yet. Next action is to run /gsd:execute-phase to execute the plans.
</current_state>

<completed_work>

- Research: 06-RESEARCH.md — Deep research on Firebase Auth, Firestore security rules, Cloud Functions setup, emulator config (Done)
- Plan 06-01: Firebase SDK setup & emulator config (Done — wave 1, no dependencies)
- Plan 06-02: Auth store, Google sign-in, route guards, onboarding redirect (Done — wave 2, depends on 06-01)
- Plan 06-03: Firestore security rules deployment + Cloud Functions scaffold (Done — wave 2, depends on 06-01)
</completed_work>

<remaining_work>

- Task 1 (Plan 06-01): Execute Firebase SDK installation, src/boot/firebase.js, emulator config — wave 1
- Task 2 (Plan 06-02): Execute auth store, signInWithGoogle, route guards, LandingPage wiring — wave 2
- Task 3 (Plan 06-03): Execute Firestore rules deployment, functions/index.js secrets & region — wave 2
</remaining_work>

<decisions_made>

- TRD.md at project root is the authoritative spec — no additional research needed for implementation
- UI/design must NOT change — only wire backend logic into existing components
- Firebase MCP server to be used for Firestore operations and deployment
- Cloud Functions: Node.js runtime, Gemini 1.5 Flash model, region: africa-south1
- MozPayments primary gateway; Stripe test mode for development fallback
- Web Speech API (SpeechRecognition) for voice — text fallback for non-Chrome browsers
- All API keys in .env / .env.local — never hardcoded
- Plan 06-03 preserves Node 24 engine spec in functions/package.json (NOT changed to 18)
</decisions_made>

<blockers>

- Firebase project must be created and Blaze (pay-as-you-go) plan enabled before execution — required for Cloud Functions
- Gemini API key needed (aistudio.google.com) — required for 06-03 Cloud Functions scaffold
- MozPayments API key — required for 06-03 Cloud Functions scaffold
- Without real credentials, 06-01 can run against emulators, but 06-02 Google sign-in and 06-03 deployment steps will need real Firebase project config
</blockers>

<context>
Milestone v1.1 backend kicked off 2026-02-23. Phase 6 covers the Firebase Auth infrastructure layer — the foundation everything else in v1.1 depends on. Plans follow a wave pattern: 06-01 runs first (SDK/emulator setup), then 06-02 and 06-03 run in parallel (both depend only on 06-01). The existing UI components (LandingPage.vue, OnboardingPage.vue) are already built — Phase 6 just wires real auth behind them by replacing the mock store with a real Firebase Auth store. No UI changes allowed.
</context>

<next_action>
Start with: /gsd:execute-phase — select phase 06-firebase-auth. Verify Firebase project + Blaze plan + .env.local credentials are ready before beginning execution. If credentials are not ready, 06-01 emulator setup can still run but 06-02 and 06-03 will need the real project config.
</next_action>
