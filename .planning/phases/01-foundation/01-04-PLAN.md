---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified: []
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "The app shell is complete and verified by a human — all 6 FOUND requirements are confirmed working"
    - "Design tokens, dark mode, Inter font, routing, stores, and axios strip are all confirmed in a live browser"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 1 foundation. Confirms all six requirements are live and working in the browser before Phase 2 begins building on top of this infrastructure.

Purpose: Phase 2 will immediately start using themes, routes, and stores. A broken foundation silently corrupts every subsequent phase. One focused verification session here catches issues at the lowest possible cost.
Output: Human approval that the foundation is solid. No files modified.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 1 foundation — all 6 FOUND requirements in live browser</name>
  <action>Present the human verification checklist below and pause for human confirmation. No files are modified. This checkpoint verifies all work from Plans 01, 02, and 03 is functioning correctly before Phase 2 begins.</action>
  <what-built>
Complete Phase 1 foundation:
- Plan 01: Design tokens (quasar.config.js brand config, CSS vars, Inter font, dark mode auto, FOUC fix)
- Plan 02: Two-layout routing (MainLayout with bottom nav, FullscreenLayout without), all 8 named routes, 8 page stubs
- Plan 03: Three Pinia stores (auth, profile, session) with mock data, axios boot stripped
  </what-built>
  <how-to-verify>
Run `npx quasar dev` and open the app in your browser. Work through this checklist:

**FOUND-01 — Design Tokens:**
1. Open DevTools Elements panel, select the `<body>` element
2. In Computed styles, scroll to CSS custom properties — confirm: `--primary: #4cae4f`, `--accent-orange: #FF6B35`, `--radius-sm: 0.5rem`
3. If any QBtn is visible, confirm it renders with green color (not default blue)

**FOUND-02 — Dark/Light Mode:**
4. Set your OS to dark mode — the app background should go dark (near-black #151d15) with NO white flash on load
5. Set your OS to light mode — the app background should go white/light
6. Check DevTools Elements: `<body>` should have class `body--dark` in dark mode, `body--light` in light mode

**FOUND-03 — Mobile Container:**
7. Resize browser to 1200px wide (desktop size)
8. Confirm the app content is centered and constrained to ~430px width, not stretched full-width

**FOUND-04 — Routing:**
9. Navigate to each URL and confirm the correct stub text appears, NO 404:
   - `/#/` — "Landing Page"
   - `/#/onboarding` — "Onboarding"
   - `/#/session` — "Active Session"
   - `/#/feedback` — "Session Feedback"
   - `/#/app/dashboard` — "Dashboard" (WITH bottom nav: Home, Progress, Profile)
   - `/#/app/progress` — "Your Progress" (WITH bottom nav, Progress highlighted)
   - `/#/app/vocabulary` — "Vocabulary Bank" (WITH bottom nav)
   - `/#/app/profile` — "Profile & Settings" (WITH bottom nav, Profile highlighted)
10. Navigate to `/#/doesnotexist` — confirm the ErrorNotFound page appears
11. On a page with bottom nav, confirm the tab icons look like Material Symbols (rounded, outlined style — NOT the filled square Material Icons style)

**FOUND-05 — Pinia Stores:**
12. Open Vue DevTools (or install if needed) — Pinia panel
13. Confirm 3 stores exist: auth, profile, session
14. auth store: isAuthenticated = false, hasCompletedOnboarding = false
15. profile store: displayName = "Sarah Chen", streakDays = 12, totalSessions = 47
16. session store: isActive = false, overallScore = null

**FOUND-06 — No HTTP Calls:**
17. Open DevTools Network tab, filter by "XHR" and "Fetch"
18. Navigate between all 8 pages
19. Confirm: ZERO XHR/Fetch requests appear (Google Fonts CDN request in "All" tab is expected and fine — filter to XHR/Fetch to exclude it)
  </how-to-verify>
  <verify>Human confirms all 19 checks pass by typing "approved".</verify>
  <done>Human has typed "approved" — all 6 FOUND requirements confirmed working in a live browser session.</done>
  <resume-signal>Type "approved" if all 19 checks pass. Or describe any specific failures so they can be fixed before Phase 2 begins.</resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification. Human approval of all 19 checks constitutes successful Phase 1 completion.
</verification>

<success_criteria>
Human has verified all 6 FOUND requirements in a live browser:
- FOUND-01: CSS vars confirmed in DevTools, QBtn renders green
- FOUND-02: Dark/light OS switching works with correct colors and no flash
- FOUND-03: Content stays within 430px on wide screens
- FOUND-04: All 8 named routes resolve, bottom nav shows/hides correctly per layout
- FOUND-05: 3 Pinia stores present with correct mock data
- FOUND-06: Zero XHR/Fetch network requests on page navigation
</success_criteria>

<output>
After approval, create `.planning/phases/01-foundation/01-04-SUMMARY.md` following the summary template.

Then update STATE.md:
- Phase: 2 of 5 (Entry Flow)
- Plan: 0 of 2 in current phase
- Status: Ready to plan
- Last activity: Phase 1 Foundation complete — design tokens, routing, stores, and mock data layer verified
</output>
