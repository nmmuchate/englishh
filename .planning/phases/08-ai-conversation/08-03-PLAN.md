---
phase: 08-ai-conversation
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: []
autonomous: false
requirements: [CONV-01, CONV-02, CONV-03, CONV-04, CONV-05, FUNC-01, FUNC-02]

must_haves:
  truths:
    - "AI conversation loop works end-to-end with real Gemini responses"
    - "Paywall correctly blocks session 2+ for non-subscribed users"
    - "Voice input works in Chrome/Edge with interim transcript visible"
    - "Text fallback works on non-Chrome browsers"
  artifacts:
    - path: "src/pages/SessionPage.vue"
      provides: "Live conversation UI"
    - path: "functions/index.js"
      provides: "Deployed startConversation + sendMessage"
  key_links:
    - from: "SessionPage mic button"
      to: "SpeechRecognition API"
      via: "recognition.start() in toggleMic"
      pattern: "recognition.start"
    - from: "SpeechRecognition onresult"
      to: "session.sendMessage"
      via: "sendUserMessage() call on isFinal"
      pattern: "sendUserMessage"
---

<objective>
Human verification that the Phase 8 AI conversation engine works end-to-end: real Gemini responses appear in the transcript, the paywall gate fires correctly, and both voice and text input paths are functional.

Purpose: Confirm the core product loop (speak → AI responds) works before moving to Phase 9.
Output: Verified conversation loop ready for Phase 9 session scoring.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 8 AI conversation engine end-to-end</name>
  <files>none (verification only)</files>
  <action>Human verifies the complete Phase 8 AI conversation engine using the steps in how-to-verify. No code changes — this is a blocking gate that confirms startConversation + sendMessage Cloud Functions, Web Speech API, text fallback, and paywall gate all work correctly before Phase 9 begins.</action>
  <verify>Run all 5 manual verification tests described in how-to-verify below.</verify>
  <done>All 5 tests pass: AI topic + opening message on session start, voice sends to Gemini, text fallback works, paywall blocks non-subscribed users, no console errors on happy path.</done>
  <what-built>
Complete Phase 8 AI Conversation Engine:
- startConversation Cloud Function (Gemini topic assignment, session doc creation)
- sendMessage Cloud Function (Gemini conversation, transcript update, mistake detection)
- Web Speech API integration in SessionPage (mic button → real voice capture)
- Text input fallback (keyboard toggle or auto-shown on non-Chrome)
- Paywall gate in SessionPage (blocks session start for non-subscribed users who used free session)
- Live transcript renders real AI responses from Gemini
  </what-built>
  <how-to-verify>
**Prerequisites:** Firebase emulators running (`firebase emulators:start`) and GEMINI_API_KEY set.

**Test 1 — startConversation (Cloud Function):**
1. Start the app: `quasar dev`
2. Sign in and navigate to the dashboard
3. Tap "Start Session"
4. Expected: Session page loads, topic appears in header (AI-generated), first AI message appears in transcript within 3 seconds
5. Expected: No hardcoded "Ordering at a Cafe" text

**Test 2 — sendMessage (voice input, Chrome/Edge only):**
1. On the session page, tap the green mic button
2. Expected: Mic button shows active state (glow ring), caption shows "LISTENING..."
3. Speak a sentence in English
4. Expected: Interim transcript shows your words while speaking (greyed-out bubble)
5. After speaking stops: Expected: Your message appears in transcript (green bubble), AI response appears within 3 seconds
6. Expected: Mistake counter increments if any grammar mistakes detected

**Test 3 — text input fallback:**
1. Tap the keyboard icon button (bottom left) to toggle text input
2. Expected: QInput field appears above the footer buttons
3. Type a message and press Enter or tap the send button
4. Expected: Message appears in transcript, AI responds

**Test 4 — paywall gate:**
1. In Firestore emulator: set `users/{uid}.freeSessionUsed = true` and `subscriptionStatus = 'none'`
2. Navigate to session and tap Start Session
3. Expected: PaywallDialog opens instead of starting the session

**Test 5 — error resilience:**
1. Start a session and send several messages
2. Expected: Conversation flows naturally, no JS console errors on happy path
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed and what was observed</resume-signal>
</task>

</tasks>

<verification>
All 5 manual verification tests pass:
1. Session starts with AI-generated topic and opening message
2. Voice input captures speech and sends to Gemini (Chrome/Edge)
3. Text input fallback works via keyboard toggle
4. Paywall gate fires for non-subscribed users with freeSessionUsed=true
5. Conversation continues cleanly through multiple exchanges
</verification>

<success_criteria>
- AI conversation loop works end-to-end with real Gemini 1.5 Flash responses
- Both voice (Chrome/Edge) and text input paths work correctly
- Paywall gate correctly blocks session 2+ for users without active subscription
- Phase 9 (Session Scoring) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-conversation/08-03-SUMMARY.md`
</output>
