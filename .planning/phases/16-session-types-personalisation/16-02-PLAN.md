---
phase: 16-session-types-personalisation
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/pages/SessionTypeSelectPage.vue
  - src/components/SessionTypeCard.vue
  - src/pages/ScenarioBriefPage.vue
autonomous: true
requirements:
  - SESSION-01
  - SESSION-02

must_haves:
  truths:
    - "User sees SessionTypeSelectPage listing all 4 session types with correct names, icons, and descriptions"
    - "A1-A2 users see Story Builder and Debate cards grayed out with lock icons"
    - "B1+ users see all 4 cards unlocked with primary borders"
    - "Tapping a locked card shows a QNotify snackbar and does NOT navigate"
    - "Tapping an unlocked card shows a loading spinner, calls startSession(type) on session store, then navigates to /session/brief"
    - "ScenarioBriefPage displays topic, role, context, and objectives from sessionStore.sessionPlan"
    - "Tapping Start Session on ScenarioBriefPage navigates to /session"
  artifacts:
    - path: "src/pages/SessionTypeSelectPage.vue"
      provides: "Session type selection page with 2x2 grid and loading overlay"
      min_lines: 80
    - path: "src/components/SessionTypeCard.vue"
      provides: "Reusable session type card with locked/unlocked variants"
      min_lines: 40
    - path: "src/pages/ScenarioBriefPage.vue"
      provides: "Pre-session briefing page with role, context, objectives, and Start Session CTA"
      min_lines: 60
  key_links:
    - from: "src/pages/SessionTypeSelectPage.vue"
      to: "src/stores/session.js"
      via: "sessionStore.startSession(type)"
      pattern: "startSession"
    - from: "src/pages/SessionTypeSelectPage.vue"
      to: "src/stores/placement.js"
      via: "placementStore.finalResult?.overallLevel for CEFR lock check"
      pattern: "finalResult"
    - from: "src/pages/ScenarioBriefPage.vue"
      to: "src/stores/session.js"
      via: "sessionStore.sessionPlan for brief content"
      pattern: "sessionPlan"
    - from: "src/pages/ScenarioBriefPage.vue"
      to: "src/router/routes.js"
      via: "router.push({ name: 'session' }) on Start Session tap"
      pattern: "name: 'session'"
---

<objective>
Create the SessionTypeSelectPage, SessionTypeCard component, and ScenarioBriefPage — the UI layer for session type selection and pre-session briefing.

Purpose: Users can now choose from 4 session types, see lock states based on their CEFR level, and view a personalised briefing before starting a session. This completes the Phase 16 user-facing flow.

Output: Three new Vue files delivering SESSION-01 (view types) and SESSION-02 (view brief).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-session-types-personalisation/16-CONTEXT.md
@.planning/phases/16-session-types-personalisation/16-UI-SPEC.md
@.planning/phases/16-session-types-personalisation/16-01-SUMMARY.md

@src/stores/session.js
@src/stores/placement.js
@src/stores/profile.js

<interfaces>
<!-- Key types and contracts from Plan 01 that executor needs -->

From src/stores/session.js (after Plan 01 refactor):
```javascript
const sessionPlan = ref(null)   // { topic, role, context, objectives, systemPrompt }
const sessionType = ref(null)   // 'free-talk' | 'scenario' | 'story-builder' | 'debate'
const sessionId = ref(null)     // set after generateSessionPlan returns

async function startSession(type) {
  // type: 'free-talk' | 'scenario' | 'story-builder' | 'debate'
  // Returns: { paywallRequired: boolean, error?: string }
  // On success: sessionId, sessionPlan, topic, transcript[0] are populated
}
```

From src/stores/placement.js:
```javascript
const finalResult = ref(null)  // { overallLevel: 'B1', levelName, skillBreakdown, strengths, weaknesses, confidence }
```

From src/stores/profile.js:
```javascript
const currentLevel = ref(null)       // 'B1', 'B2', etc. — fallback if placement not done
const subscriptionStatus = ref('none')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionTypeCard.vue component</name>
  <files>src/components/SessionTypeCard.vue</files>
  <read_first>
    - .planning/phases/16-session-types-personalisation/16-UI-SPEC.md (SessionTypeCard section — exact layout, locked/unlocked variants, accessibility)
    - .planning/phases/16-session-types-personalisation/16-CONTEXT.md (D-02 lock state presentation)
  </read_first>
  <action>
Create `src/components/SessionTypeCard.vue` as a single-file Vue 3 component with `<script setup>`.

**Props:**
```javascript
const props = defineProps({
  type: { type: String, required: true },       // 'free-talk', 'scenario', etc.
  icon: { type: String, required: true },       // Material icon name
  name: { type: String, required: true },       // Display name
  description: { type: String, required: true },// Short description
  duration: { type: String, required: true },   // e.g. '15-20 min'
  locked: { type: Boolean, default: false }     // CEFR lock state
})
```

**Emits:**
```javascript
const emit = defineEmits(['select'])
```

**Click handler:**
```javascript
function handleClick() {
  if (!props.locked) {
    emit('select', props.type)
  }
}
```
(Locked card tap handled by parent via separate click handler for the QNotify)

**Template — Unlocked card:**
```html
<div
  class="session-type-card-wrapper"
  :class="{ 'session-type-card--locked': locked }"
  role="button"
  tabindex="0"
  :aria-disabled="locked ? 'true' : undefined"
  :aria-label="locked ? `${name} — locked, available at B1` : name"
  @click="handleClick"
  @keyup.enter="handleClick"
>
  <q-card flat class="session-type-card q-pa-md" :class="{ 'session-type-card--unlocked': !locked }">
    <!-- Lock icon overlay for locked cards -->
    <q-icon
      v-if="locked"
      name="lock"
      size="20px"
      color="grey-6"
      class="lock-overlay"
    />

    <q-icon :name="icon" size="28px" :color="locked ? 'grey-6' : 'primary'" class="q-mb-xs" />
    <p class="text-subtitle1 text-weight-bold q-mb-xs q-mt-xs" style="margin: 0;">{{ name }}</p>
    <p class="text-body2 text-grey-6 q-mb-sm card-description" style="margin: 0;">{{ description }}</p>

    <div v-if="locked" class="q-mt-xs">
      <p class="text-caption text-grey-5" style="margin: 0;">Available at B1</p>
    </div>
    <div v-else>
      <q-chip dense color="primary" text-color="white" size="sm">{{ duration }}</q-chip>
    </div>
  </q-card>
</div>
```

**Scoped styles:**
```css
.session-type-card-wrapper {
  cursor: pointer;
  position: relative;
}

.session-type-card--locked {
  opacity: 0.5;
}

.session-type-card {
  position: relative;
  border-radius: 16px;
  min-height: 88px;
  border: 2px solid transparent;
}

.session-type-card--unlocked .session-type-card {
  border: 2px solid var(--q-primary);
}

.lock-overlay {
  position: absolute;
  top: 8px;
  right: 8px;
}

.card-description {
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
```

Note: The `.session-type-card--unlocked .session-type-card` selector targets the border. Because the wrapper div has the `--unlocked` class modifier, the nested q-card gets the primary border.
  </action>
  <verify>
    <automated>cd C:/Users/User/Documents/GitHub/englishh && test -f src/components/SessionTypeCard.vue && grep -q "defineProps" src/components/SessionTypeCard.vue && grep -q "role=\"button\"" src/components/SessionTypeCard.vue && grep -q "aria-disabled" src/components/SessionTypeCard.vue && grep -q "lock-overlay" src/components/SessionTypeCard.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SessionTypeCard.vue exists and has more than 40 lines
    - File contains `defineProps` with `type`, `icon`, `name`, `description`, `duration`, `locked` props
    - File contains `defineEmits(['select'])`
    - File contains `emit('select', props.type)` inside click handler
    - File contains `role="button"` and `tabindex="0"` on the wrapper element
    - File contains `aria-disabled` binding for locked cards
    - File contains `aria-label` with "locked, available at B1" text for locked cards
    - File contains `q-icon name="lock" size="20px"` with `lock-overlay` class
    - File contains `opacity: 0.5` in scoped styles for locked state
    - File contains `border: 2px solid var(--q-primary)` for unlocked card border
    - File contains `border-radius: 16px` on the card
    - File contains `min-height: 88px` on the card
    - File contains `q-chip dense color="primary"` for duration chip
    - File contains `text-caption text-grey-5` with "Available at B1" text
  </acceptance_criteria>
  <done>SessionTypeCard.vue component renders locked and unlocked variants with correct visual treatment, accessibility attributes, lock icon overlay, primary border on unlocked, opacity on locked, and emits select event on tap</done>
</task>

<task type="auto">
  <name>Task 2: Create SessionTypeSelectPage.vue and ScenarioBriefPage.vue</name>
  <files>src/pages/SessionTypeSelectPage.vue, src/pages/ScenarioBriefPage.vue</files>
  <read_first>
    - src/components/SessionTypeCard.vue (just created in Task 1 — props interface)
    - src/stores/session.js (startSession(type) signature, sessionPlan ref, sessionType ref)
    - src/stores/placement.js (finalResult.overallLevel for CEFR gate)
    - src/stores/profile.js (currentLevel fallback)
    - .planning/phases/16-session-types-personalisation/16-UI-SPEC.md (SessionTypeSelectPage + ScenarioBriefPage screen contracts)
    - .planning/phases/16-session-types-personalisation/16-CONTEXT.md (D-02 lock tap notification, D-04 flow)
  </read_first>
  <action>
**SessionTypeSelectPage.vue:**

Create `src/pages/SessionTypeSelectPage.vue` as a single-file Vue 3 component with `<script setup>`.

**Imports:**
```javascript
import { ref, computed } from 'vue'
import { useRouter } from 'vue-router'
import { useQuasar } from 'quasar'
import { useSessionStore } from 'stores/session'
import { usePlacementStore } from 'stores/placement'
import { useProfileStore } from 'stores/profile'
import SessionTypeCard from 'components/SessionTypeCard.vue'
```

**Session type definitions (constant array):**
```javascript
const SESSION_TYPES = [
  { type: 'free-talk', icon: 'chat', name: 'Free Talk', description: 'Open conversation on any topic', duration: '15\u201320 min', minCefr: 1 },
  { type: 'scenario', icon: 'work', name: 'Scenario', description: 'Role-play a real-world situation', duration: '15\u201320 min', minCefr: 1 },
  { type: 'story-builder', icon: 'menu_book', name: 'Story Builder', description: 'Build a story together with AI', duration: '20\u201325 min', minCefr: 3 },
  { type: 'debate', icon: 'gavel', name: 'Debate', description: 'Argue your position on a topic', duration: '20\u201325 min', minCefr: 3 }
]
```
(minCefr: 1 = A1, 3 = B1 — matches CEFR_VALUE mapping)

**CEFR level computed:**
```javascript
const CEFR_VALUE = { A1: 1, A2: 2, B1: 3, B2: 4, C1: 5, C2: 6 }

const userCefrNumeric = computed(() => {
  const level = placementStore.finalResult?.overallLevel || profileStore.currentLevel || 'B1'
  return CEFR_VALUE[level] ?? 3
})
```

**State:**
```javascript
const isGenerating = ref(false)
const generateError = ref(null)
```

**isLocked helper:**
```javascript
function isLocked(sessionDef) {
  return userCefrNumeric.value < sessionDef.minCefr
}
```

**handleSelectType handler:**
```javascript
async function handleSelectType(type) {
  isGenerating.value = true
  generateError.value = null

  const result = await sessionStore.startSession(type)

  if (result?.paywallRequired) {
    isGenerating.value = false
    // TODO: open PaywallDialog — Phase 18 will handle this
    return
  }

  if (result?.error) {
    isGenerating.value = false
    generateError.value = result.error
    return
  }

  isGenerating.value = false
  router.push({ name: 'session-brief' })
}
```

**handleLockedTap handler:**
```javascript
function handleLockedTap(sessionDef) {
  $q.notify({
    message: `${sessionDef.name} unlocks at B1`,
    type: 'warning',
    position: 'top'
  })
}
```

**Template:**
```html
<q-page class="q-pa-md">
  <div style="max-width: 430px; margin: 0 auto;">
    <!-- Heading -->
    <h2 class="text-h6 text-weight-bold q-mb-xs">Choose Your Session</h2>
    <p class="text-body2 text-grey-6 q-mb-lg">Select the type of English practice you want today.</p>

    <!-- Session type grid -->
    <div class="row q-col-gutter-sm q-mb-xl">
      <div v-for="st in SESSION_TYPES" :key="st.type" class="col-6">
        <div v-if="isLocked(st)" @click="handleLockedTap(st)">
          <SessionTypeCard
            :type="st.type"
            :icon="st.icon"
            :name="st.name"
            :description="st.description"
            :duration="st.duration"
            :locked="true"
          />
        </div>
        <SessionTypeCard
          v-else
          :type="st.type"
          :icon="st.icon"
          :name="st.name"
          :description="st.description"
          :duration="st.duration"
          :locked="false"
          @select="handleSelectType"
        />
      </div>
    </div>

    <!-- Error banner -->
    <q-banner v-if="generateError" class="bg-red-1 text-red-9 q-mb-lg" rounded>
      <template v-slot:default>
        Couldn't prepare your session. Tap Retry or choose a different type.
      </template>
      <template v-slot:action>
        <q-btn flat no-caps label="Back" class="text-grey-5" @click="generateError = null" />
        <q-btn flat no-caps label="Retry" color="primary" @click="handleSelectType(sessionStore.sessionType)" />
      </template>
    </q-banner>

    <!-- Loading overlay -->
    <q-inner-loading :showing="isGenerating">
      <q-spinner-dots size="64px" color="primary" />
      <p class="text-body2 text-grey-6 q-mt-md text-center">Preparing your session...</p>
    </q-inner-loading>
  </div>
</q-page>
```

No scoped styles needed — all styling via Quasar utility classes.

---

**ScenarioBriefPage.vue:**

Create `src/pages/ScenarioBriefPage.vue` as a single-file Vue 3 component with `<script setup>`.

**Imports:**
```javascript
import { computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useSessionStore } from 'stores/session'
```

**Guards:**
```javascript
const router = useRouter()
const sessionStore = useSessionStore()

const plan = computed(() => sessionStore.sessionPlan)
const typeName = computed(() => {
  const map = { 'free-talk': 'Free Talk', 'scenario': 'Scenario', 'story-builder': 'Story Builder', 'debate': 'Debate' }
  return map[sessionStore.sessionType] || 'Session'
})

onMounted(() => {
  if (!plan.value) {
    router.replace({ name: 'sessions' })
  }
})
```

**Navigation handlers:**
```javascript
function startSession() {
  router.push({ name: 'session' })
}

function goBack() {
  router.push({ name: 'sessions' })
}
```

**Template:**
```html
<q-page class="q-pa-lg">
  <div v-if="plan" style="max-width: 430px; margin: 0 auto;">
    <!-- Session type badge -->
    <q-chip color="primary" text-color="white" class="q-mb-md">{{ typeName }}</q-chip>

    <!-- Topic title -->
    <h2 class="text-h6 text-weight-bold q-mb-lg">{{ plan.topic }}</h2>

    <!-- Your Role section -->
    <div class="text-overline text-grey-5 q-mb-xs">YOUR ROLE</div>
    <q-card flat class="q-pa-md q-mb-md">
      <p class="text-body2" style="margin: 0;">{{ plan.role }}</p>
    </q-card>

    <!-- Context section -->
    <div class="text-overline text-grey-5 q-mb-xs">CONTEXT</div>
    <q-card flat class="q-pa-md q-mb-md">
      <p class="text-body2" style="margin: 0;">{{ plan.context }}</p>
    </q-card>

    <!-- Objectives section -->
    <div class="text-overline text-grey-5 q-mb-xs">OBJECTIVES</div>
    <q-card flat class="q-pa-md q-mb-xl">
      <div v-for="(obj, i) in plan.objectives" :key="i" class="row items-start q-mb-sm">
        <q-icon name="check_circle" color="primary" size="16px" class="q-mr-xs" aria-hidden="true" />
        <span class="text-body2">{{ obj }}</span>
      </div>
    </q-card>

    <!-- Primary CTA -->
    <q-btn
      label="Start Session"
      color="primary"
      unelevated
      rounded
      no-caps
      size="lg"
      class="full-width"
      @click="startSession"
    />

    <!-- Back link -->
    <q-btn
      flat
      no-caps
      label="Choose Different Type"
      class="full-width q-mt-sm text-grey-5"
      @click="goBack"
    />
  </div>
</q-page>
```

No scoped styles needed.
  </action>
  <verify>
    <automated>cd C:/Users/User/Documents/GitHub/englishh && test -f src/pages/SessionTypeSelectPage.vue && test -f src/pages/ScenarioBriefPage.vue && grep -q "SessionTypeCard" src/pages/SessionTypeSelectPage.vue && grep -q "isGenerating" src/pages/SessionTypeSelectPage.vue && grep -q "sessionPlan" src/pages/ScenarioBriefPage.vue && grep -q "Start Session" src/pages/ScenarioBriefPage.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/SessionTypeSelectPage.vue exists and has more than 80 lines
    - src/pages/ScenarioBriefPage.vue exists and has more than 60 lines
    - SessionTypeSelectPage contains `import SessionTypeCard from 'components/SessionTypeCard.vue'`
    - SessionTypeSelectPage contains `usePlacementStore` and `useSessionStore` imports
    - SessionTypeSelectPage contains `SESSION_TYPES` array with 4 entries (free-talk, scenario, story-builder, debate)
    - SessionTypeSelectPage contains `isLocked` function that compares `userCefrNumeric` to `minCefr`
    - SessionTypeSelectPage contains `handleSelectType` function calling `sessionStore.startSession(type)`
    - SessionTypeSelectPage contains `handleLockedTap` function calling `$q.notify`
    - SessionTypeSelectPage contains `q-inner-loading :showing="isGenerating"`
    - SessionTypeSelectPage contains `q-spinner-dots size="64px" color="primary"`
    - SessionTypeSelectPage contains `"Preparing your session..."` loading text
    - SessionTypeSelectPage contains `"Choose Your Session"` heading
    - SessionTypeSelectPage contains `class="col-6"` for 2x2 grid layout
    - SessionTypeSelectPage contains `q-banner` with `"Couldn't prepare your session"` error text
    - SessionTypeSelectPage contains `router.push({ name: 'session-brief' })` on success
    - SessionTypeSelectPage contains `$q.notify` with `unlocks at B1` message pattern
    - ScenarioBriefPage contains `sessionStore.sessionPlan` read
    - ScenarioBriefPage contains `YOUR ROLE` section heading
    - ScenarioBriefPage contains `CONTEXT` section heading
    - ScenarioBriefPage contains `OBJECTIVES` section heading
    - ScenarioBriefPage contains `q-icon name="check_circle"` with `aria-hidden="true"`
    - ScenarioBriefPage contains `q-btn label="Start Session" color="primary"` with `size="lg"` and `class="full-width"`
    - ScenarioBriefPage contains `"Choose Different Type"` back button label
    - ScenarioBriefPage contains `router.replace({ name: 'sessions' })` redirect when plan is null
    - ScenarioBriefPage contains `q-chip color="primary"` for session type badge
  </acceptance_criteria>
  <done>SessionTypeSelectPage shows 2x2 grid of 4 session types with CEFR lock states, loading overlay during plan generation, error banner with retry, and navigates to /session/brief on success. ScenarioBriefPage displays topic, role, context, objectives from session store with Start Session CTA and Choose Different Type back link.</done>
</task>

</tasks>

<verification>
1. `test -f src/components/SessionTypeCard.vue` — component file exists
2. `test -f src/pages/SessionTypeSelectPage.vue` — page file exists
3. `test -f src/pages/ScenarioBriefPage.vue` — page file exists
4. `grep -c "SessionTypeCard" src/pages/SessionTypeSelectPage.vue` returns at least 2 (import + usage)
5. `grep "col-6" src/pages/SessionTypeSelectPage.vue` confirms 2x2 grid
6. `grep "opacity: 0.5" src/components/SessionTypeCard.vue` confirms locked visual
7. `grep "startSession" src/pages/SessionTypeSelectPage.vue` confirms store integration
8. `grep "session-brief" src/pages/SessionTypeSelectPage.vue` confirms navigation
9. `grep "sessionPlan" src/pages/ScenarioBriefPage.vue` confirms plan data binding
10. `grep "YOUR ROLE" src/pages/ScenarioBriefPage.vue` confirms brief section structure
</verification>

<success_criteria>
- SessionTypeSelectPage renders 4 session type cards in a 2x2 grid (SESSION-01)
- Locked cards (Story Builder, Debate for A1-A2) have opacity 0.5 + lock icon + "Available at B1" text
- Tapping locked card triggers QNotify warning snackbar with "{Type} unlocks at B1" message
- Tapping unlocked card triggers loading overlay + generateSessionPlan call
- On success, user navigates to ScenarioBriefPage showing topic, role, context, objectives (SESSION-02)
- ScenarioBriefPage "Start Session" CTA navigates to /session with sessionId already in store
- All accessibility attributes present (role="button", tabindex, aria-disabled, aria-label, aria-hidden)
</success_criteria>

<output>
After completion, create `.planning/phases/16-session-types-personalisation/16-02-SUMMARY.md`
</output>
