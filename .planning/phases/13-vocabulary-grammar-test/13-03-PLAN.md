---
phase: 13-vocabulary-grammar-test
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/pages/GrammarStage.vue
autonomous: true
requirements:
  - PLACE-04
must_haves:
  truths:
    - "User sees 4-6 error-spotting questions where they tap the incorrect word and type a correction"
    - "User sees 2-3 sentence-completion questions where they type the missing words"
    - "Answering 2 questions correctly in a row increases adaptiveLevel by one CEFR step"
    - "Answering 2 questions incorrectly in a row decreases adaptiveLevel by one CEFR step"
    - "A progress bar shows how many questions remain"
    - "When all questions are answered, GrammarStage emits @complete with { score, level, answers }"
    - "A loading skeleton is shown while generateTestQuestions is fetching"
    - "A retry button is shown if the Cloud Function call fails"
  artifacts:
    - path: "src/pages/GrammarStage.vue"
      provides: "Grammar placement test stage component"
      contains: "generateTestQuestions"
      contains_secondary: "error-spot"
  key_links:
    - from: "src/pages/GrammarStage.vue"
      to: "generateTestQuestions Cloud Function"
      via: "httpsCallable from functions boot"
      pattern: "generateTestQuestions"
    - from: "src/pages/GrammarStage.vue"
      to: "parent OnboardingPage.vue"
      via: "emit('complete', { score, level, answers })"
      pattern: "emit.*complete"
---

<objective>
Create `src/pages/GrammarStage.vue` — the Grammar placement test stage. This component calls `generateTestQuestions` (type='grammar'), renders error-spotting and sentence-completion questions, implements the same adaptive difficulty engine as VocabularyStage, and emits `@complete` with the scored result.

Purpose: PLACE-04 — users must take a grammar test with error-spotting and sentence-completion questions rendered from GPT-4o-mini output, with adaptive difficulty adjustments.

Output: New `src/pages/GrammarStage.vue` (~180 lines), slotted into OnboardingPage in Plan 13-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/pages/OnboardingPage.vue
@src/stores/placement.js
@src/boot/firebase.js

<interfaces>
<!-- Cloud Function contract — returned by generateTestQuestions(type='grammar') -->
{
  type: 'grammar',
  level: 'B1',
  questions: [
    // Error-spot questions (4 items, id: g1..g4):
    {
      id: 'g1',
      kind: 'error-spot',
      sentence: 'She don\'t like coffee in the morning.',
      errorWord: 'don\'t',         // verbatim substring in sentence
      correction: 'doesn\'t',
      explanation: 'Third-person singular present simple requires \'doesn\'t\'',
      cefrLevel: 'B1'
    },
    // Sentence-completion questions (2 items, id: g5..g6):
    {
      id: 'g5',
      kind: 'sentence-completion',
      stem: 'By the time she arrived, we _______ (already/leave).',
      answer: 'had already left',    // exact expected string (case-insensitive compare on check)
      cefrLevel: 'B2'
    }
  ]
}

<!-- ERROR-SPOT UI pattern: -->
// 1. Show the full sentence with the errorWord highlighted (use a span with background color)
// 2. A QInput below: label="Type the correction", v-model="userCorrection"
// 3. "Check" QBtn — compares userCorrection.trim().toLowerCase() to correction.toLowerCase()
// 4. Show green (correct) or red (wrong) feedback inline, then "Next" btn

<!-- SENTENCE-COMPLETION UI pattern: -->
// 1. Show the stem with _______ displayed as a blank (styled span)
// 2. A QInput below: label="Fill in the blank", v-model="userAnswer"
// 3. "Check" QBtn — compares userAnswer.trim().toLowerCase() to answer.toLowerCase()
// 4. Show feedback, then "Next" btn

<!-- ADAPTIVE DIFFICULTY — identical logic to VocabularyStage: -->
const CEFR_ORDER = ['A1', 'A2', 'B1', 'B2', 'C1', 'C2']
// 2 correct in a row → bump up; 2 wrong in a row → bump down

<!-- emit contract — identical shape to VocabularyStage: -->
emit('complete', { score: 0-100, level: 'B1', answers: [{ id, correct: boolean }] })
emit('skip')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GrammarStage.vue with error-spot and sentence-completion flow</name>
  <read_first>
    - src/pages/VocabularyStage.vue (read the FULL file — GrammarStage follows the same structure: fetchQuestions on mount, adaptive engine, emit contract, loading/error states — replicate the pattern, do not diverge)
    - src/pages/OnboardingPage.vue (confirm @complete and @skip emit expectations)
    - src/boot/firebase.js (confirm functions export)
  </read_first>
  <files>src/pages/GrammarStage.vue</files>
  <action>
Create `src/pages/GrammarStage.vue` using `<script setup>` + Vue 3 Composition API.

**Structure mirrors VocabularyStage.vue exactly** for: imports, CEFR_ORDER, adaptiveLevel, adaptive counters, fetchQuestions, progress computed, loading/error states, skip emit. The differences are in the question rendering and answer checking logic.

**Script setup — additional refs beyond VocabularyStage:**
```js
const userInput = ref('')     // v-model for QInput on current question (correction or fill-in)
const showFeedback = ref(false)
const lastAnswerCorrect = ref(false)
```

**fetchQuestions:** Same as VocabularyStage but `type: 'grammar'`:
```js
const result = await fn({ type: 'grammar', level: adaptiveLevel.value, userProfile })
questions.value = result.data.questions  // flat array of error-spot + sentence-completion
```

**handleCheck function** (replaces handleAnswer — user types then taps Check):
```js
function handleCheck() {
  if (showFeedback.value || !userInput.value.trim()) return
  const q = currentQuestion.value
  let isCorrect = false

  if (q.kind === 'error-spot') {
    isCorrect = userInput.value.trim().toLowerCase() === q.correction.toLowerCase()
  } else if (q.kind === 'sentence-completion') {
    isCorrect = userInput.value.trim().toLowerCase() === q.answer.toLowerCase()
  }

  lastAnswerCorrect.value = isCorrect
  showFeedback.value = true
  answers.value.push({ id: q.id, correct: isCorrect })

  // Adaptive difficulty (identical to VocabularyStage)
  if (isCorrect) {
    consecutiveCorrect.value++
    consecutiveWrong.value = 0
    if (consecutiveCorrect.value >= 2) {
      const idx = CEFR_ORDER.indexOf(adaptiveLevel.value)
      if (idx < CEFR_ORDER.length - 1) adaptiveLevel.value = CEFR_ORDER[idx + 1]
      consecutiveCorrect.value = 0
    }
  } else {
    consecutiveWrong.value++
    consecutiveCorrect.value = 0
    if (consecutiveWrong.value >= 2) {
      const idx = CEFR_ORDER.indexOf(adaptiveLevel.value)
      if (idx > 0) adaptiveLevel.value = CEFR_ORDER[idx - 1]
      consecutiveWrong.value = 0
    }
  }
}
```

**handleNext:** Same as VocabularyStage — advance currentIndex or emit complete:
```js
function handleNext() {
  showFeedback.value = false
  userInput.value = ''
  lastAnswerCorrect.value = false
  if (currentIndex.value < allQuestions.value.length - 1) {
    currentIndex.value++
  } else {
    const totalAnswered = answers.value.length
    const totalCorrect = answers.value.filter(a => a.correct).length
    const score = totalAnswered > 0 ? Math.round((totalCorrect / totalAnswered) * 100) : 0
    emit('complete', { score, level: adaptiveLevel.value, answers: answers.value })
  }
}
```

**allQuestions computed:** Same as VocabularyStage — `questions.value` (no passage for grammar):
```js
const allQuestions = computed(() => questions.value)
```

**Template structure:**

Loading, error, and skip states: identical to VocabularyStage (q-skeleton, q-banner with retry, skip link).

Question card differs by kind:

```html
<!-- Error-spot question layout -->
<template v-if="currentQuestion.kind === 'error-spot'">
  <p class="text-overline text-grey-5 q-mb-xs">SPOT THE ERROR</p>
  <p class="text-subtitle1 text-weight-medium q-mb-md" style="line-height: 1.7;">
    <!-- Highlight the errorWord by splitting on it and wrapping in a span -->
    <template v-for="(part, i) in splitSentence(currentQuestion)" :key="i">
      <span
        v-if="i === 1"
        :class="showFeedback ? (lastAnswerCorrect ? 'text-positive text-strike' : 'text-negative text-strike') : 'text-negative bg-red-1'"
        style="padding: 2px 4px; border-radius: 4px; font-weight: 600;"
      >{{ part }}</span>
      <span v-else>{{ part }}</span>
    </template>
  </p>
  <q-input
    v-model="userInput"
    label="Type the correction"
    outlined
    rounded
    class="q-mb-md"
    :disable="showFeedback"
    @keyup.enter="handleCheck"
  />
  <!-- Feedback message -->
  <div v-if="showFeedback" class="q-mb-md">
    <p v-if="lastAnswerCorrect" class="text-positive text-weight-medium">Correct!</p>
    <p v-else class="text-negative text-weight-medium">
      The correct answer is: <strong>{{ currentQuestion.correction }}</strong>
    </p>
    <p v-if="!lastAnswerCorrect" class="text-caption text-grey-6">{{ currentQuestion.explanation }}</p>
  </div>
</template>

<!-- Sentence-completion question layout -->
<template v-else-if="currentQuestion.kind === 'sentence-completion'">
  <p class="text-overline text-grey-5 q-mb-xs">COMPLETE THE SENTENCE</p>
  <p class="text-subtitle1 text-weight-medium q-mb-md" style="line-height: 1.7;">
    {{ currentQuestion.stem }}
  </p>
  <q-input
    v-model="userInput"
    label="Fill in the blank"
    outlined
    rounded
    class="q-mb-md"
    :disable="showFeedback"
    @keyup.enter="handleCheck"
  />
  <div v-if="showFeedback" class="q-mb-md">
    <p v-if="lastAnswerCorrect" class="text-positive text-weight-medium">Correct!</p>
    <p v-else class="text-negative text-weight-medium">
      The correct answer is: <strong>{{ currentQuestion.answer }}</strong>
    </p>
  </div>
</template>

<!-- Check / Next buttons -->
<q-btn
  v-if="!showFeedback"
  label="Check"
  color="primary"
  unelevated
  rounded
  no-caps
  class="full-width"
  :disable="!userInput.trim()"
  @click="handleCheck"
/>
<q-btn
  v-if="showFeedback"
  :label="currentIndex < allQuestions.length - 1 ? 'Next Question' : 'See Results'"
  color="primary"
  unelevated
  rounded
  no-caps
  class="full-width"
  @click="handleNext"
/>
```

**splitSentence helper function** (splits sentence around errorWord for highlighting):
```js
function splitSentence(q) {
  const idx = q.sentence.indexOf(q.errorWord)
  if (idx === -1) return [q.sentence, '', '']
  return [
    q.sentence.slice(0, idx),
    q.errorWord,
    q.sentence.slice(idx + q.errorWord.length)
  ]
}
```
  </action>
  <verify>
    <automated>grep -n "httpsCallable\|error-spot\|sentence-completion\|handleCheck\|consecutiveCorrect\|emit.*complete\|splitSentence" C:/Users/User/Documents/GitHub/englishh/src/pages/GrammarStage.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "httpsCallable" src/pages/GrammarStage.vue` returns a line with the import
    - `grep -n "generateTestQuestions" src/pages/GrammarStage.vue` returns a line inside fetchQuestions with `type: 'grammar'`
    - `grep -n "error-spot" src/pages/GrammarStage.vue` returns lines in template for the question layout
    - `grep -n "sentence-completion" src/pages/GrammarStage.vue` returns lines in template for fill-in layout
    - `grep -n "handleCheck" src/pages/GrammarStage.vue` returns at least 2 lines (function definition and @click binding)
    - `grep -n "splitSentence" src/pages/GrammarStage.vue` returns at least 2 lines (function + template usage)
    - `grep -n "consecutiveCorrect\|consecutiveWrong" src/pages/GrammarStage.vue` returns at least 4 lines
    - `grep -n "emit.*complete" src/pages/GrammarStage.vue` returns a line in handleNext
    - File exists at `src/pages/GrammarStage.vue`
  </acceptance_criteria>
  <done>GrammarStage.vue exists, calls generateTestQuestions with type='grammar' on mount, renders error-spot questions with word highlighting + correction input, renders sentence-completion questions with fill-in input, implements identical adaptive difficulty engine to VocabularyStage, emits @complete with score+level+answers when finished</done>
</task>

</tasks>

<verification>
- `grep -c "adaptiveLevel" src/pages/GrammarStage.vue` returns >= 4
- `grep "type: 'grammar'" src/pages/GrammarStage.vue` — exists in fetchQuestions call
- `grep "emit('complete'" src/pages/GrammarStage.vue` — exists in handleNext
</verification>

<success_criteria>
GrammarStage.vue exists and mirrors VocabularyStage.vue structure. It calls `generateTestQuestions` with type='grammar', renders error-spotting (with word highlighting) and sentence-completion (with text input) question types, adapts difficulty on 2-consecutive rules, and emits `@complete` with `{ score, level, answers }` on completion.
</success_criteria>

<output>
After completion, create `.planning/phases/13-vocabulary-grammar-test/13-03-SUMMARY.md`
</output>
