---
phase: 13-vocabulary-grammar-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/index.js
autonomous: true
requirements:
  - PLACE-08
must_haves:
  truths:
    - "generateTestQuestions Cloud Function is callable and returns a correctly structured payload"
    - "Calling with type='vocabulary' and level='B1' returns 6-8 MCQ questions plus one reading passage with 2-3 comprehension questions"
    - "Calling with type='grammar' and level='B1' returns 4-6 error-spotting questions and 2-3 sentence-completion questions"
    - "Each vocabulary MCQ has exactly 4 options and one is marked correct"
    - "Each grammar error-spotting question includes the full sentence, the errorWord, and its correction"
    - "Each sentence-completion question includes the stem and the expected answer"
    - "An invalid type argument throws an HttpsError with code 'invalid-argument'"
  artifacts:
    - path: "functions/index.js"
      provides: "generateTestQuestions onCall export"
      contains: "exports.generateTestQuestions"
  key_links:
    - from: "functions/index.js (generateTestQuestions)"
      to: "OpenAI gpt-4o-mini"
      via: "openai.chat.completions.create with response_format json_object"
      pattern: "generateTestQuestions"
---

<objective>
Add the `generateTestQuestions` HTTPS callable Cloud Function to `functions/index.js`. This function accepts `{ type, level, userProfile }` and returns AI-generated question payloads used by both the Vocabulary/Reading stage (Plan 13-02) and the Grammar stage (Plan 13-03).

Purpose: PLACE-08 — questions must be generated dynamically at test time via GPT-4o-mini to prevent memorisation and enable adaptive difficulty personalised to the user's interests.

Output: A new exported `generateTestQuestions` function appended to `functions/index.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Established Cloud Function patterns in this codebase (from functions/index.js) -->
<!-- Use onCall, region: 'africa-south1', secrets: [OPENAI_API_KEY] — exactly as other functions -->
<!-- OpenAI instantiated INSIDE handler body — OPENAI_API_KEY.value() only resolves during invocation -->
<!-- response_format: { type: 'json_object' } on every OpenAI call — prevents hallucinated structure -->
<!-- Throw HttpsError for bad args, unauthenticated, or OpenAI failure -->

<!-- RETURN CONTRACT — VocabularyStage and GrammarStage components will import httpsCallable -->
<!-- and destructure exactly this shape from the response.data: -->

// type === 'vocabulary' return shape:
{
  type: 'vocabulary',
  level: 'B1',         // echoed back — the level that was generated at
  questions: [         // 6-8 items
    {
      id: 'v1',
      kind: 'mcq',
      sentence: 'The project was _______ due to budget constraints.',
      options: ['postponed', 'deleted', 'forgotten', 'removed'],
      correctIndex: 0,   // 0-3
      cefrLevel: 'B1'    // level this question tests
    }
    // ...more MCQ questions...
  ],
  passage: {
    text: '50-80 word paragraph',
    questions: [         // 2-3 items
      {
        id: 'p1',
        kind: 'comprehension',
        question: 'Why did Maria move to London?',
        options: ['For a job', 'For family', 'For studies', 'For travel'],
        correctIndex: 0,
        cefrLevel: 'B1'
      }
    ]
  }
}

// type === 'grammar' return shape:
{
  type: 'grammar',
  level: 'B1',
  questions: [
    // Error-spotting questions (4-6):
    {
      id: 'g1',
      kind: 'error-spot',
      sentence: 'She don\'t like coffee in the morning.',
      errorWord: 'don\'t',
      correction: 'doesn\'t',
      explanation: 'Third-person singular present simple requires \'doesn\'t\'',
      cefrLevel: 'B1'
    },
    // Sentence-completion questions (2-3):
    {
      id: 'g5',
      kind: 'sentence-completion',
      stem: 'By the time she arrived, we _______ (already/leave).',
      answer: 'had already left',
      cefrLevel: 'B2'
    }
  ]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generateTestQuestions Cloud Function</name>
  <read_first>
    - functions/index.js (read the FULL file — must append after the last export, using the exact require pattern, secret declaration, and region already established)
  </read_first>
  <files>functions/index.js</files>
  <action>
Append the following new export to the bottom of `functions/index.js`, after the `updateWeeklyLeaderboard` export. Do NOT modify any existing code.

The function must:

1. Be declared as: `exports.generateTestQuestions = onCall({ region: 'africa-south1', secrets: [OPENAI_API_KEY] }, async (request) => { ... })`

2. Validate auth: `if (!request.auth) throw new HttpsError('unauthenticated', 'Authentication required')`

3. Destructure: `const { type, level = 'B1', userProfile = {} } = request.data`

4. Validate type: `if (!['vocabulary', 'grammar'].includes(type)) throw new HttpsError('invalid-argument', 'type must be vocabulary or grammar')`

5. Instantiate OpenAI inside handler body: `const openai = new OpenAI({ apiKey: OPENAI_API_KEY.value() })`

6. For `type === 'vocabulary'`, call `openai.chat.completions.create` with:
   - model: `'gpt-4o-mini'`
   - temperature: `0.7`
   - max_tokens: `1500`
   - response_format: `{ type: 'json_object' }`
   - system message: `'You are an English language test generator. Always respond with valid JSON only. Generate questions appropriate for the CEFR level specified.'`
   - user message (use template literal):
     ```
     Generate a vocabulary and reading comprehension test for CEFR level ${level}.
     User context: ${JSON.stringify(userProfile)} — use topics relevant to the user's interests/occupation when possible.

     Return JSON with exactly this structure:
     {
       "type": "vocabulary",
       "level": "${level}",
       "questions": [
         {
           "id": "v1",
           "kind": "mcq",
           "sentence": "sentence with _______ for the target word",
           "options": ["option1", "option2", "option3", "option4"],
           "correctIndex": 0,
           "cefrLevel": "${level}"
         }
       ],
       "passage": {
         "text": "50-80 word paragraph",
         "questions": [
           {
             "id": "p1",
             "kind": "comprehension",
             "question": "question text",
             "options": ["A", "B", "C", "D"],
             "correctIndex": 0,
             "cefrLevel": "${level}"
           }
         ]
       }
     }

     Rules:
     - Include exactly 6 vocabulary MCQ questions (id: v1..v6) — start at ${level} then vary by ±1 CEFR level
     - Include exactly 1 passage with exactly 2 comprehension questions (id: p1, p2)
     - Each MCQ must have exactly 4 options with exactly one correct answer
     - options array must contain strings only, no null values
     - correctIndex must be 0, 1, 2, or 3
     ```

7. For `type === 'grammar'`, call `openai.chat.completions.create` with:
   - model: `'gpt-4o-mini'`
   - temperature: `0.7`
   - max_tokens: `1200`
   - response_format: `{ type: 'json_object' }`
   - system message: same as above
   - user message (use template literal):
     ```
     Generate a grammar test for CEFR level ${level}.
     User context: ${JSON.stringify(userProfile)} — use topics relevant to the user's interests/occupation when possible.

     Return JSON with exactly this structure:
     {
       "type": "grammar",
       "level": "${level}",
       "questions": [
         {
           "id": "g1",
           "kind": "error-spot",
           "sentence": "full sentence with ONE grammatical error",
           "errorWord": "the incorrect word or phrase",
           "correction": "the correct replacement",
           "explanation": "brief explanation of the rule",
           "cefrLevel": "${level}"
         },
         {
           "id": "g5",
           "kind": "sentence-completion",
           "stem": "sentence with _______ blank",
           "answer": "correct answer",
           "cefrLevel": "${level}"
         }
       ]
     }

     Rules:
     - Include exactly 4 error-spot questions (id: g1..g4) — start at ${level}
     - Include exactly 2 sentence-completion questions (id: g5..g6)
     - error-spot: each sentence has exactly ONE error; errorWord must appear verbatim in sentence
     - sentence-completion: stem must contain exactly one _______
     ```

8. Wrap parse in try/catch — on JSON parse failure throw `new HttpsError('internal', 'Question generation failed — please retry')`

9. Return the parsed object directly (it already matches the contract shape).

10. Add a logger.info call on success: `logger.info('generateTestQuestions complete', { type, level, uid: request.auth.uid })`
  </action>
  <verify>
    <automated>cd C:/Users/User/Documents/GitHub/englishh/functions && node -e "const f = require('./index.js'); console.log(typeof f.generateTestQuestions)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "exports.generateTestQuestions" functions/index.js` returns a line
    - `grep -n "invalid-argument" functions/index.js` returns at least 2 lines (one for type validation, one existing)
    - `grep -n "error-spot" functions/index.js` returns a line inside the grammar prompt
    - `grep -n "sentence-completion" functions/index.js` returns a line inside the grammar prompt
    - `grep -n "passage" functions/index.js` returns lines inside the vocabulary prompt and return contract
    - Running `node -e "const f = require('./index.js'); console.log(typeof f.generateTestQuestions)"` prints `function`
  </acceptance_criteria>
  <done>generateTestQuestions is exported from functions/index.js, validates auth and type arg, calls gpt-4o-mini with structured prompts for both vocabulary and grammar types, returns the correct payload shape or throws HttpsError on failure</done>
</task>

</tasks>

<verification>
- `grep "exports.generateTestQuestions" functions/index.js` — confirms export exists
- `grep "error-spot" functions/index.js` — confirms grammar error-spot prompt
- `grep "passage" functions/index.js` — confirms vocabulary reading passage structure
- `node -e "require('./functions/index.js')" 2>&1 | head -5` — no require-time errors
</verification>

<success_criteria>
`generateTestQuestions` is deployed (or deployable) and returns correctly structured vocabulary + grammar question payloads. Both stage UI components (Plans 13-02 and 13-03) can call this function via httpsCallable and destructure the response without additional transformation.
</success_criteria>

<output>
After completion, create `.planning/phases/13-vocabulary-grammar-test/13-01-SUMMARY.md`
</output>
