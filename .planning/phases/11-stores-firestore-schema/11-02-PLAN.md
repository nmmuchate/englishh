---
phase: 11-stores-firestore-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/scripts/seed-scenario-library.js
autonomous: true
requirements:
  - INFRA-v12-03
user_setup:
  - service: firebase-admin
    why: "Seed script requires authenticated access to Firestore"
    env_vars:
      - name: GOOGLE_APPLICATION_CREDENTIALS
        source: "Firebase Console -> Project Settings -> Service Accounts -> Generate New Private Key (download JSON, set env var to path)"
    dashboard_config: []

must_haves:
  truths:
    - "scenarioLibrary Firestore collection contains at least 40 scenario template documents (10 categories x 4 session types)"
    - "Seed script is idempotent — running it twice does not create duplicate documents"
    - "Every scenario document has all required fields: title, type, targetLevel, targetSkills, grammarFocus, relevantFields, relevantInterests, systemPrompt, aiRole, userRole, objectives, successCriteria"
  artifacts:
    - path: "functions/scripts/seed-scenario-library.js"
      provides: "Idempotent Admin SDK seed script for scenarioLibrary collection"
      min_lines: 200
  key_links:
    - from: "functions/scripts/seed-scenario-library.js"
      to: "scenarioLibrary/{levelId}/scenarios/{scenarioId}"
      via: "admin.firestore().collection('scenarioLibrary').doc(level).collection('scenarios').doc(id)"
      pattern: "scenarioLibrary.*scenarios"
---

<objective>
Create the idempotent Node.js Admin SDK seed script that populates the scenarioLibrary Firestore collection with at least 40 scenario templates covering all 4 session types across 10 field/interest categories.

Purpose: Provides the scenario content that Phase 16 (Session Types) reads to populate session type selection and pre-session briefing screens.
Output: One seed script file runnable via `node functions/scripts/seed-scenario-library.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-stores-firestore-schema/11-CONTEXT.md
@.planning/phases/11-stores-firestore-schema/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent scenarioLibrary seed script with 40 scenario templates</name>
  <files>functions/scripts/seed-scenario-library.js</files>
  <read_first>
    - functions/package.json (confirm firebase-admin is installed, check Node engine version)
    - .planning/phases/11-stores-firestore-schema/11-RESEARCH.md (exact scenario document schema, seed coverage matrix, idempotency pattern)
    - .planning/phases/11-stores-firestore-schema/11-CONTEXT.md (D-02: seed full library this phase, D-03: idempotent Admin SDK script, D-04: minimum coverage matrix)
  </read_first>
  <action>
Create `functions/scripts/seed-scenario-library.js` as a CommonJS Node.js script.

**Header comment:**
```
// Seed script for scenarioLibrary Firestore collection.
// Idempotent: checks if each document exists before writing.
// Prerequisite: Set GOOGLE_APPLICATION_CREDENTIALS env var to service account JSON path,
//   OR run `firebase login` for Application Default Credentials.
// Usage: node functions/scripts/seed-scenario-library.js
// Dry run: node functions/scripts/seed-scenario-library.js --dry-run
```

**Structure:**
1. `const admin = require('firebase-admin')` — require at top
2. `if (!admin.apps.length) admin.initializeApp()` — idempotent init
3. `const db = admin.firestore()`
4. Parse `--dry-run` flag from `process.argv`
5. Define the `scenarios` array containing exactly 40 objects (10 categories x 4 types)

**Each scenario object MUST have ALL of these fields** (per PRD schema):
- `id`: string — pattern: `{field-kebab}-{type}-{level}` e.g. `engineering-scenario-b1`
- `title`: string — descriptive scenario title
- `type`: string — one of: `freeTalk`, `scenario`, `storyBuilder`, `debate`
- `targetLevel`: string — CEFR level: `A1`, `A2`, `B1`, `B2`, `C1`, or `C2`
- `targetSkills`: string array — e.g. `['speaking', 'vocabulary']`
- `grammarFocus`: string array — e.g. `['present_simple', 'modal_verbs']`
- `relevantFields`: string array — e.g. `['Engineering']`
- `relevantInterests`: string array — e.g. `['Technology']`
- `systemPrompt`: string — full system prompt for Gemini/GPT with `{{userName}}` placeholder where applicable
- `aiRole`: string — e.g. `'Your manager at a software company'`
- `userRole`: string — e.g. `'A software engineer explaining a production outage'`
- `objectives`: string array — 2-4 learning objectives
- `successCriteria`: string — what constitutes successful completion

**Coverage matrix (D-04) — create exactly one scenario per cell (40 total):**

| Category | Type | Level | ID |
|----------|------|-------|----|
| Engineering | scenario | B1 | `engineering-scenario-b1` |
| Engineering | freeTalk | B1 | `engineering-freetalk-b1` |
| Engineering | storyBuilder | B2 | `engineering-storybuilder-b2` |
| Engineering | debate | B2 | `engineering-debate-b2` |
| Health | scenario | B1 | `health-scenario-b1` |
| Health | freeTalk | A2 | `health-freetalk-a2` |
| Health | storyBuilder | B1 | `health-storybuilder-b1` |
| Health | debate | B2 | `health-debate-b2` |
| Business | scenario | B1 | `business-scenario-b1` |
| Business | freeTalk | B1 | `business-freetalk-b1` |
| Business | storyBuilder | B1 | `business-storybuilder-b1` |
| Business | debate | B2 | `business-debate-b2` |
| Technology | scenario | B1 | `technology-scenario-b1` |
| Technology | freeTalk | B1 | `technology-freetalk-b1` |
| Technology | storyBuilder | B1 | `technology-storybuilder-b1` |
| Technology | debate | B2 | `technology-debate-b2` |
| Student | scenario | A2 | `student-scenario-a2` |
| Student | freeTalk | A2 | `student-freetalk-a2` |
| Student | storyBuilder | A2 | `student-storybuilder-a2` |
| Student | debate | B1 | `student-debate-b1` |
| Travel | scenario | B1 | `travel-scenario-b1` |
| Travel | freeTalk | A2 | `travel-freetalk-a2` |
| Travel | storyBuilder | B1 | `travel-storybuilder-b1` |
| Travel | debate | B2 | `travel-debate-b2` |
| Gaming | scenario | B1 | `gaming-scenario-b1` |
| Gaming | freeTalk | A2 | `gaming-freetalk-a2` |
| Gaming | storyBuilder | B1 | `gaming-storybuilder-b1` |
| Gaming | debate | B1 | `gaming-debate-b1` |
| Cooking | scenario | A2 | `cooking-scenario-a2` |
| Cooking | freeTalk | A2 | `cooking-freetalk-a2` |
| Cooking | storyBuilder | A2 | `cooking-storybuilder-a2` |
| Cooking | debate | B1 | `cooking-debate-b1` |
| Sports | scenario | B1 | `sports-scenario-b1` |
| Sports | freeTalk | A2 | `sports-freetalk-a2` |
| Sports | storyBuilder | B1 | `sports-storybuilder-b1` |
| Sports | debate | B1 | `sports-debate-b1` |
| Music | scenario | B1 | `music-scenario-b1` |
| Music | freeTalk | A2 | `music-freetalk-a2` |
| Music | storyBuilder | B1 | `music-storybuilder-b1` |
| Music | debate | B1 | `music-debate-b1` |

**System prompts must be substantive** — at least 2-3 sentences describing the AI's persona, the conversation context, and how to adapt to the user's level. Include `{{userName}}` placeholder where the AI should address the user by name.

**Seed function:**
```javascript
async function seed() {
  const isDryRun = process.argv.includes('--dry-run')
  let wrote = 0, skipped = 0

  for (const scenario of scenarios) {
    const ref = db
      .collection('scenarioLibrary')
      .doc(scenario.targetLevel)
      .collection('scenarios')
      .doc(scenario.id)

    const snap = await ref.get()
    if (snap.exists) {
      console.log(`SKIP ${scenario.id} (already exists)`)
      skipped++
      continue
    }

    if (isDryRun) {
      console.log(`DRY-RUN would write ${scenario.id} -> scenarioLibrary/${scenario.targetLevel}/scenarios/${scenario.id}`)
      wrote++
      continue
    }

    // Write a copy without the `id` field (Firestore doc ID is the id)
    const { id, ...docData } = scenario
    await ref.set(docData)
    console.log(`WROTE ${scenario.id} -> scenarioLibrary/${scenario.targetLevel}/scenarios/${scenario.id}`)
    wrote++
  }

  console.log(`\nDone. Wrote: ${wrote}, Skipped: ${skipped}, Total: ${scenarios.length}`)
}

seed()
  .then(() => process.exit(0))
  .catch((err) => { console.error('Seed failed:', err); process.exit(1) })
```

The `id` field is stripped from the document data before writing (used only as the document ID in the path).

**Do NOT:**
- Hardcode a Firebase project ID — let Admin SDK infer from ADC
- Use ESM imports — this is CJS (`require`)
- Skip the idempotency check (`snap.exists`) per D-03
- Use batch writes — sequential writes with existence check are required for idempotency logging
  </action>
  <verify>
    <automated>grep -c "scenarioLibrary" functions/scripts/seed-scenario-library.js && grep "snap.exists" functions/scripts/seed-scenario-library.js && grep "dry-run\|--dry-run" functions/scripts/seed-scenario-library.js && node -e "const s = require('fs').readFileSync('functions/scripts/seed-scenario-library.js','utf8'); const m = s.match(/id:\s*'/g); console.log('scenario count:', m ? m.length : 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `functions/scripts/seed-scenario-library.js` exists
    - File starts with `const admin = require('firebase-admin')`
    - File contains `if (!admin.apps.length) admin.initializeApp()`
    - File contains `const db = admin.firestore()`
    - File contains `snap.exists` (idempotency check per D-03)
    - File contains `--dry-run` flag support
    - File contains exactly 40 scenario objects (count of `id: '` occurrences equals 40)
    - Every scenario has all required fields: `grep "title:" functions/scripts/seed-scenario-library.js | wc -l` >= 40
    - Every scenario has `systemPrompt` field: `grep "systemPrompt:" functions/scripts/seed-scenario-library.js | wc -l` >= 40
    - File contains `scenarioLibrary` in the Firestore path
    - File contains `process.exit(0)` and `process.exit(1)` for clean exit
    - Coverage: grep for all 10 categories present: engineering, health, business, technology, student, travel, gaming, cooking, sports, music
    - Coverage: grep for all 4 types present: freeTalk, scenario, storyBuilder, debate
  </acceptance_criteria>
  <done>Seed script exists at functions/scripts/seed-scenario-library.js, contains 40 scenario templates covering all 10 categories and 4 session types, is idempotent (checks existence before writing), supports --dry-run, and uses Admin SDK CJS pattern.</done>
</task>

</tasks>

<verification>
1. `node -e "const s = require('fs').readFileSync('functions/scripts/seed-scenario-library.js','utf8'); const m = s.match(/id:\s*'/g); console.log(m.length)"` outputs 40
2. `grep "snap.exists" functions/scripts/seed-scenario-library.js` exits 0
3. `grep "dry-run" functions/scripts/seed-scenario-library.js` exits 0
4. `grep "engineering\|health\|business\|technology\|student\|travel\|gaming\|cooking\|sports\|music" functions/scripts/seed-scenario-library.js | head -10` shows multiple categories
</verification>

<success_criteria>
- Seed script is runnable with `node functions/scripts/seed-scenario-library.js`
- Script contains 40 scenario templates (10 categories x 4 session types)
- Script is idempotent (checks snap.exists before writing)
- Script supports --dry-run flag
- Every scenario document has all required schema fields
- Script uses CJS require pattern with firebase-admin
</success_criteria>

<output>
After completion, create `.planning/phases/11-stores-firestore-schema/11-02-SUMMARY.md`
</output>
