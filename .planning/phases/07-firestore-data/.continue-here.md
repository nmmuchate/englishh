---
phase: 07-firestore-data
task: 4
total_tasks: 7
status: in_progress
last_updated: 2026-02-25T09:05:19.925Z
---

<current_state>
Phase 07 plan 03 is complete and committed. Ready to start plan 07-04. The vocabulary data layer is fully wired (VocabularyPage + FeedbackPage). Remaining work covers offline persistence (DATA-07) and session Firestore sync (DATA-08).
</current_state>

<completed_work>

- Plan 07-01: Firebase boot setup, profile service wired to boot/auth.js, auth store integration — Done
- Plan 07-02: OnboardingPage writes, DashboardPage + ProgressPage wired to profileStore real Firestore data — Done
- Plan 07-03: Vocabulary data layer — vocabulary service + store created, VocabularyPage reads from Firestore subcollection, FeedbackPage "Add to Bank" button saves words — Done (commits c97e888, a9213d9)
</completed_work>

<remaining_work>

- Plan 07-04 (next): DATA-07 — offline persistence via `initializeFirestore` + `persistentLocalCache` (replaces deprecated `enableIndexedDbPersistence` in boot/firebase.js); DATA-08 — `useSessionStore.startSession()` writes session metadata to `sessions/{sessionId}` in Firestore
- Plans 07-05 through 07-07: TBD based on any remaining DATA-* requirements or integration cleanup
</remaining_work>

<decisions_made>

- DATA-07 must use `initializeFirestore(app, { localCache: persistentLocalCache() })` NOT `enableIndexedDbPersistence()` — the latter is deprecated in firebase@12.9.0; requires replacing `getFirestore(app)` call in boot/firebase.js (one-line change, must happen before connectFirestoreEmulator)
- DATA-08: `addDoc` to `sessions` collection in `startSession()`, store returned doc ref for subsequent updates in Phase 9
- FeedbackPage static vocabWords array is intentionally retained — Phase 9 replaces with real session scoring data; 07-03 only added the save affordance
</decisions_made>

<blockers>

- None currently — Firebase project should already be on Blaze plan from Phase 6 setup
</blockers>

<context>
Phase 07 removes all hardcoded/mock data and wires every page to real Firestore. The boot/auth.js onAuthStateChanged pattern is established — profile data flows through useProfileStore. The vocabulary subcollection pattern (`vocabulary/{uid}/words`) is working. Two DATA items remain: offline cache config and session document creation. These are small, surgical changes — 07-04 should be fast (~2 plans). After phase 07, the app reads/writes all its core data to Firestore. Phase 08 is the AI conversation engine.
</context>

<next_action>
Start with: `/gsd:plan-phase` for plan 07-04, targeting DATA-07 (offline persistence in boot/firebase.js) and DATA-08 (session Firestore write in useSessionStore.startSession). Research already exists in 07-RESEARCH.md — no new research needed.
</next_action>
